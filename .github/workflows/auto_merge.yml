name: Auto Merge

on:
  push:
    branches:
      - develop

jobs:
  create_pull_request:
    name: "Create a pull requests"
    runs-on: ubuntu-latest
    steps:
      - name: "🚚 - Checkout develop"
        uses: actions/checkout@v2

      - name: "💫 - Create a Pull Request develop to develop-G1"
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: develop-G1
          branch: develop
          title: "Merge develop to develop-G1"
          body: "A merge conflict occurred when trying to merge develop into develop-G1. Please resolve the conflict and merge the changes manually."
          delete-branch: false

      - name: "💫 - Create a Pull Request develop to develop-G2"
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: develop-G2
          branch: develop
          title: "Merge develop to develop-G2"
          body: "A merge conflict occurred when trying to merge develop into develop-G2. Please resolve the conflict and merge the changes manually."
          delete-branch: false

      - name: "💫 - Create a Pull Request develop to develop-G5"
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: develop-G5
          branch: develop
          title: "Merge develop to develop-G5"
          body: "A merge conflict occurred when trying to merge develop into develop-G5. Please resolve the conflict and merge the changes manually."
          delete-branch: false

      - name: "💫 - Create a Pull Request develop to develop-G6"
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: develop-G6
          branch: develop
          title: "Merge develop to develop-G6"
          body: "A merge conflict occurred when trying to merge develop into develop-G6. Please resolve the conflict and merge the changes manually."
          delete-branch: false

      - name: "💫 - Create a Pull Request develop to develop-G7"
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: develop-G7
          branch: develop
          title: "Merge develop to develop-G7"
          body: "A merge conflict occurred when trying to merge develop into develop-G7. Please resolve the conflict and merge the changes manually."
          delete-branch: false

      - name: "💫 - Create a Pull Request develop to develop-G8"
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: develop-G8
          branch: develop
          title: "Merge develop to develop-G8"
          body: "A merge conflict occurred when trying to merge develop into develop-G8. Please resolve the conflict and merge the changes manually."
          delete-branch: false

      - name: "💫 - Create a Pull Request develop to develop-G10"
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: develop-G10
          branch: develop
          title: "Merge develop to develop-G10"
          body: "A merge conflict occurred when trying to merge develop into develop-G10. Please resolve the conflict and merge the changes manually."
          delete-branch: false

  merge_branchs:
    name: "Auto Merge to all child branches"
    runs-on: ubuntu-latest
    needs: create_pull_request
    steps:
      - name: "🚚 - Checkout develop"
        uses: actions/checkout@v2

      - name: "🔀 - Merge develop to develop G1"
        uses: mtanzi/action-automerge@v1
        id: mergeG1
        with:
          github_token: ${{ github.token }}
          source: 'develop'
          target: 'develop-G1'
          webhook_url: ${{ secrets.WEBHOOK_URL }}

      - name: "🔀 - Merge develop to develop G2"
        uses: mtanzi/action-automerge@v1
        id: mergeG2
        with:
          github_token: ${{ github.token }}
          source: 'develop'
          target: 'develop-G2'
          webhook_url: ${{ secrets.WEBHOOK_URL }}

      - name: "🔀 - Merge develop to develop G5"
        uses: mtanzi/action-automerge@v1
        id: mergeG5
        with:
          github_token: ${{ github.token }}
          source: 'develop'
          target: 'develop-G5'
          webhook_url: ${{ secrets.WEBHOOK_URL }}

      - name: "🔀 - Merge develop to develop G6"
        uses: mtanzi/action-automerge@v1
        id: mergeG6
        with:
          github_token: ${{ github.token }}
          source: 'develop'
          target: 'develop-G6'
          webhook_url: ${{ secrets.WEBHOOK_URL }}

      - name: "🔀 - Merge develop to develop G7"
        uses: mtanzi/action-automerge@v1
        id: mergeG7
        with:
          github_token: ${{ github.token }}
          source: 'develop'
          target: 'develop-G7'
          webhook_url: ${{ secrets.WEBHOOK_URL }}

      - name: "🔀 - Merge develop to develop G8"
        uses: mtanzi/action-automerge@v1
        id: mergeG8
        with:
          github_token: ${{ github.token }}
          source: 'develop'
          target: 'develop-G8'
          webhook_url: ${{ secrets.WEBHOOK_URL }}

      - name: "🔀 - Merge develop to develop G10"
        uses: mtanzi/action-automerge@v1
        id: mergeG10
        with:
          github_token: ${{ github.token }}
          source: 'develop'
          target: 'develop-G10'
          webhook_url: ${{ secrets.WEBHOOK_URL }}
